<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Le Go par l'exemple</a>: {{.Name}}</title>
    <link rel=stylesheet href="site.css">
  </head>
  <body>
    <div class="example" id="{{.Id}}">
      <h2><a href="./">Le Go par l'exemple</a>: {{.Name}}</h2>
      {{range .Segs}}
      <table>
        {{range .}}
        <tr>
          <td class="docs">
            {{.DocsRendered}}
          </td>
          <td class="code{{if .CodeEmpty}} empty{{end}}{{if .CodeLeading}} leading{{end}}">
	        {{if .CodeRun}}<a href="http://play.golang.org/p/{{$.UrlHash}}"><img title="Exécuter le code" src="play.png" class="run" /></a>{{end}}
            {{.CodeRendered}}
          </td>
        </tr>
        {{end}}
      </table>
      {{end}}
      {{if .NextExample}}
      <p class="next">
        Exemple suivant: <a href="{{.NextExample.Id}}.html">{{.NextExample.Name}}</a>.
      </p>
      {{end}}
      <p class="footer">
        par <a href="https://twitter.com/clemkeirua">@clemkeirua</a> de <a href="http://www.keiruaprod.fr">KeiruaProd</a> | <a href="mailto:clement@keiruaprod.fr">retours</a> | <a href="https://github.com/Keirua/gobyexample">source</a> | <a href="https://github.com/Keirua/gobyexample#license">license</a>
      </p>
    </div>
  </body>
</html>
