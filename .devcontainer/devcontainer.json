{
    "updateContentCommand": "curl -fsSL https://github.com/gohugoio/hugo/releases/download/v0.142.0/hugo_extended_0.142.0_linux-amd64.tar.gz -o \"${t=$(mktemp --suffix .tar.gz)}\" && sudo mkdir -p /usr/local/hugo/bin && sudo tar -xzf \"$t\" -C /usr/local/hugo/bin && rm \"$t\""
}